name: Final Assignment
on: push
jobs: 
  build: 
    runs-on: ubuntu-latest
    steps: 
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: build
        run: |
          chmod +x gradlew
          ./gradlew build
        
    

